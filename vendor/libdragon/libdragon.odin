// Handwritten bindings for the narrow libdragon surface used by the N64 tracer.
//
// These declarations are pinned to the installed libdragon SDK at commit
// c79a52b42ac790e06e797aede43914dd8754cd5f. Do not update them from a different
// checkout without re-running the C layout and runtime ABI probes.
package libdragon
